# Full local dependencies (camera + face recognition + YOLO).
# Use this for full on-device setup, not Streamlit Cloud.
opencv-python>=4.8.0
face-recognition>=1.3.0
numpy>=1.24.0
pillow>=10.0.0
Flask>=3.0.0
setuptools>=80,<81
waitress>=3.0.0
ultralytics>=8.3.0
