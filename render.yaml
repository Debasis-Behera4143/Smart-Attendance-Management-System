services:
  - type: web
    name: smart-attendance-system
    env: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    healthCheckPath: /api/health
    envVars:
      - key: SMART_ATTENDANCE_ENV
        value: production
      - key: SMART_ATTENDANCE_HOST
        value: 0.0.0.0
