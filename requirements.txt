# Smart Attendance Management System - Requirements
# Install all dependencies using: pip install -r requirements.txt

# Core dependencies
opencv-python>=4.8.0
face-recognition>=1.3.0
numpy>=1.24.0
pillow>=10.0.0

# Web framework
Flask>=3.0.0

# Face recognition backend (use dlib-bin for Windows without CMake)
# For Windows: pip install dlib-bin
# For Linux/Mac: pip install dlib

# Additional dependencies
# face_recognition_models currently imports pkg_resources, so keep setuptools < 81.
setuptools>=80,<81
waitress>=3.0.0

# YOLO-based face detection backend
# Provides more accurate and faster face detection
ultralytics>=8.3.0

# Database
# SQLite is included with Python, no external dependency needed
