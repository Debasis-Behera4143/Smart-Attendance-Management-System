# Presentation-safe dependency set for Windows usage.
# Install with:
#   .\.venv\Scripts\python.exe -m pip install --no-deps -r requirements-presentation.txt

# Pin setuptools below 81 because face_recognition_models currently imports pkg_resources.
setuptools>=80,<81

# Flask and required sub-dependencies (explicit because --no-deps is used)
Flask>=3.1.2
blinker>=1.9.0
click>=8.3.0
itsdangerous>=2.2.0
jinja2>=3.1.6
markupsafe>=3.0.3
werkzeug>=3.1.4
waitress>=3.0.2

# Vision stack
numpy>=2.2.6,<2.3
pillow>=12.1.0
opencv-python>=4.12.0.88

# Face recognition stack
face-recognition-models==0.3.0
dlib-bin==20.0.0; platform_system=="Windows"
dlib>=19.24; platform_system!="Windows"
face-recognition==1.3.0
